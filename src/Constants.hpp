#ifndef CONSTANTS_H
#define CONSTANTS_H

const int LED = 14; // led pin
const int but = 0; // butt(on) pin
const int cam = 0; //set to real pin later idk what it is rn

const int apgthres = 40; // apogee is detected after this
const int serialBaud = 115200; // self explainitory

#endif // CONSTANTS_H